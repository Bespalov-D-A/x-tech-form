export interface ITechnology {
  title: string,
  description: string,
  photo: any
}

export interface IEmployee {
  name: string;
  lastName: string;
  spec: string;
  photo: any;
  skills: string[];
}

export interface IServiceItem {
  title: string;
  description: string;
}
